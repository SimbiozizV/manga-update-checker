import styled from '@emotion/styled';

const PlateTitle = styled.div`
    font-weight: 600;
    line-height: 1.2;
    cursor: pointer;

    &:hover {
        color: #1677ff;
    }
`;

export default PlateTitle;
